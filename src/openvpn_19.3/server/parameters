# This file stores parameters to generate keys(Needed by file 'prior.sh').
# NOTE: "country_name" and "province_name" can have exactly 2 alphabets.
# Parameters should be written as second arguement everywhere

Country_name	UK
Province_name	UK
City_name	Nottingham
Origin_name	Horizon
Email		def@gmail.com

# Needed by file 'keygen.sh'
# These names give name of both the clients. Respective clients should mention the SAME name. 

Host_name1 client1
Host_name2 client2

# Needed by 'initialize_server.sh' and 'stop.sh' 
# Change the port on which openvpn server listens to. Client should also run on the same port.

Port		5060 
