# This file contaians parameters which are not any tactic specific

server_name bishneet             	#This is the name on Server machine
server_ip 128.243.35.15	 	#This is the ip of Server machine
path_to_key	/root/.ssh/newkey		#This is the full path of the Private Key created between client and server. Give path as 
							/root/.ssh/key_name so that you can do ssh as sudo
